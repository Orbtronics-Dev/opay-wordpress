/**
 * Opay Payment Button — front-end block view script.
 *
 * This file is intentionally minimal: the shortcode rendered by the PHP
 * callback already produces the correct HTML structure.  The actual
 * click-handling and checkout redirect lives in assets/public.js which is
 * enqueued by Opay_Shortcodes when the shortcode is present on the page.
 */

// No additional runtime code needed; public.js handles all interactivity.
